# RPC 协议包

此目录集中维护 monorepo 内跨应用使用的 RPC 协议，是协议定义的事实源。

- `mixin-search/v1`：文档处理与检索服务的 gRPC 契约；
- 可部署应用保留各自生成的客户端或服务端代码；
- 业务实现、运行时配置和应用私有类型不进入此目录；
- 协议变更需要保持明确版本，并由所有消费方共同验证。

协议集中管理不改变应用边界：`apps/mixin-search` 负责实现检索服务，其他应用通过生成代码消费协议。
